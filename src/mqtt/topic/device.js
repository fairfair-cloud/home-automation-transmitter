export default {
    handler: function (topicName, mqttClient, message) {
        console.log(message);
        console.log("=======================================================================")
    }
}